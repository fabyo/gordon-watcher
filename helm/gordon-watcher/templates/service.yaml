apiVersion: v1
kind: Service
metadata:
  name: {{ include "gordon-watcher.fullname" . }}
  labels:
    {{- include "gordon-watcher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "gordon-watcher.selectorLabels" . | nindent 4 }}
